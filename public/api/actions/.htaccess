# Bloquea el acceso directo a actions/*
# Deben ejecutarse solo via public/api/index.php

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
